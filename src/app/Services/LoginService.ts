import {Injectable} from '@angular/core';

@Injectable({
  privatedIn:'root',
})

export class LoginService {
    LOGIN_URL='';
    LOGOUT_URL='';

constructor() {}

login(pUserName, pPassword){
private httpClient:

}
}
